class shapes {
    constructor(shapeCol, textCol, chars) {
        this.shapeCol = shapeCol;
        this.textCol = textCol;
        this.chars = chars;
    }
}

class circle {
    constructor(shapeCol, textCol, chars){
        super(shapeCol, textCol, chars);
    }
}

class triangle {
    constructor(shapeCol, textCol, chars){
        super(shapeCol, textCol, chars);
        
    }
}

class square {
    constructor(shapeCol, textCol, chars){
        super(shapeCol, textCol, chars);
        
    }
}